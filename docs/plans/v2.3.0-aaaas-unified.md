# Plan: Agents-as-a-Service (AaaS) + SaaS Unificado v2.3.0

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Transformar AzurDesk AI en una plataforma híbrida **Agents-as-a-Service + SaaS** completamente automatizada, con orquestador Simplicio-Loop, router multi-LLM multimodal, swarm de agentes robusto, memoria continua tipo Mem0/Engram, GraphRAG híbrido, Computer Use integrado, machine learning / deep learning para soporte TI, y una interfaz enterprise. Todo debe terminar como software funcional con `.exe` comercial y documentación de venta.

**Architecture:**
- **Orquestador Simplicio-Loop central** gobierna ciclos DISCOVER→PLAN→IMPLEMENT→VERIFY→MERGE→DONE.
- **Router multi-LLM multimodal** con soporte texto, imagen y futuro audio/video.
- **Agent Swarm** con roles especializados, mensajería, claims y reputación.
- **Memoria continua (Mem0/Engram)** persiste memoria de usuario/tenant en SQLite con knowledge graph y resumen.
- **GraphRAG híbrido** conecta KB, tickets, memoria y agentes.
- **Computer Use Service** permite que agentes ejecuten acciones de escritorio vía CUA.
- **ML/Deep Learning** añade clasificación, embeddings y predicción de tickets.
- **Packaging comercial** con `.exe` vía `pkg` o `nexe`, README comercial y landing.

**Tech Stack:** Node.js 24 ESM, better-sqlite3, ws, undici, zod, natural, ml-matrix, bcryptjs, jwt.

---

## Task 1: Schema de Memoria Continua (Engram/Mem0-style)

**Objective:** Crear tablas para episodic memory, semantic memory, working memory y engrams con TTL/importance.

**Files:**
- Modify: `src/services/db.js`

**Step 1: Add schema migration**

Append to `migrations` array in `src/services/db.js`:

```sql
CREATE TABLE IF NOT EXISTS engrams (
  id TEXT PRIMARY KEY,
  tenant_id TEXT,
  user_id TEXT,
  session_id TEXT,
  type TEXT DEFAULT 'episodic',
  content TEXT,
  summary TEXT,
  vector TEXT,
  importance REAL DEFAULT 1.0,
  confidence REAL DEFAULT 1.0,
  access_count INTEGER DEFAULT 0,
  last_accessed TEXT,
  created_at TEXT,
  updated_at TEXT
);
CREATE INDEX IF NOT EXISTS idx_engrams_user ON engrams (tenant_id, user_id);
CREATE INDEX IF NOT EXISTS idx_engrams_session ON engrams (tenant_id, session_id);
CREATE INDEX IF NOT EXISTS idx_engrams_type ON engrams (tenant_id, user_id, type);

CREATE TABLE IF NOT EXISTS memory_summaries (
  id TEXT PRIMARY KEY,
  tenant_id TEXT,
  user_id TEXT,
  summary TEXT,
  based_on_count INTEGER,
  created_at TEXT
);
```

**Step 2: Run syntax check**

Run: `node --check src/services/db.js`
Expected: OK

**Step 3: Commit**

```bash
git add src/services/db.js
git commit -m "feat(db): engram-style continuous memory schema"
```

---

## Task 2: Engram Memory Service

**Objective:** Implementar memoria continua con consolidación, forgot/remember y búsqueda híbrida.

**Files:**
- Create: `src/services/engramService.js`
- Test: `tests/engram.mjs`

**Step 1: Write failing test**

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import db from '../src/services/db.js';
import engramService from '../src/services/engramService.js';

test('remember y recall', () => {
  engramService.remember({ tenant_id: 't1', user_id: 'u1', content: 'Cliente prefiere respuestas en español', type: 'semantic' });
  const r = engramService.recall({ tenant_id: 't1', user_id: 'u1', query: 'idioma preferido', topK: 3 });
  assert.ok(r.length > 0);
  assert.ok(r[0].content.includes('español'));
});

test('consolidate crea resumen', () => {
  for (let i = 0; i < 5; i++) {
    engramService.remember({ tenant_id: 't2', user_id: 'u2', content: `evento ${i}` });
  }
  const s = engramService.consolidate({ tenant_id: 't2', user_id: 'u2' });
  assert.ok(s.summary);
});
```

**Step 2: Run test to verify failure**

Run: `node --test tests/engram.mjs`
Expected: FAIL — `remember is not defined`

**Step 3: Write minimal implementation**

```js
import db from './db.js';
import { safeJson, now } from './_utils.js';
import { randomUUID } from 'crypto';
import { natural } from '../ml/similaritySearch.js';
const { WordTokenizer, PorterStemmerEs, PorterStemmer } = natural;

function stem(w) { return /[^\u0000-\u007F]/.test(w) ? PorterStemmerEs.stem(w) : PorterStemmer.stem(w); }
function tokens(t) { return (new WordTokenizer().tokenize(String(t).toLowerCase()) || []).map(stem); }
function vector(t) { const v = {}; for (const x of tokens(t)) v[x] = (v[x] || 0) + 1; return v; }
function cosine(a, b) { const keys = new Set([...Object.keys(a), ...Object.keys(b)]); let dot = 0, na = 0, nb = 0; for (const k of keys) { const va = a[k] || 0, vb = b[k] || 0; dot += va * vb; na += va * va; nb += vb * vb; } const d = Math.sqrt(na) * Math.sqrt(nb); return d ? dot / d : 0; }

class EngramService {
  remember({ tenant_id, user_id, session_id, content, type = 'episodic', importance = 1.0, confidence = 1.0, summary = null }) {
    const id = randomUUID();
    db.prepare('INSERT INTO engrams (id, tenant_id, user_id, session_id, type, content, summary, vector, importance, confidence, access_count, last_accessed, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)')
      .run(id, tenant_id, user_id, session_id || null, type, content, summary, JSON.stringify(vector(content)), importance, confidence, 0, now(), now(), now());
    return { id };
  }

  recall({ tenant_id, user_id, query, type = null, topK = 5 }) {
    const qv = vector(query);
    let sql = 'SELECT * FROM engrams WHERE tenant_id=? AND (user_id=? OR (? IS NULL AND user_id IS NULL))';
    const params = [tenant_id, user_id, user_id];
    if (type) { sql += ' AND type=?'; params.push(type); }
    sql += ' ORDER BY updated_at DESC LIMIT 200';
    const rows = db.prepare(sql).all(...params);
    return rows.map(r => ({ ...r, vector: safeJson(r.vector, {}), score: cosine(qv, safeJson(r.vector, {})) * r.importance * r.confidence }))
      .sort((a, b) => b.score - a.score)
      .slice(0, topK);
  }

  recallForTenant({ tenant_id, query, topK = 5 }) {
    const qv = vector(query);
    const rows = db.prepare('SELECT * FROM engrams WHERE tenant_id=? ORDER BY updated_at DESC LIMIT 200').all(tenant_id);
    return rows.map(r => ({ ...r, score: cosine(qv, safeJson(r.vector, {})) * r.importance * r.confidence }))
      .sort((a, b) => b.score - a.score).slice(0, topK);
  }

  access(id) {
    db.prepare('UPDATE engrams SET access_count = access_count + 1, last_accessed = ? WHERE id=?').run(now(), id);
  }

  forget({ tenant_id, user_id, before }) {
    const sql = before
      ? 'DELETE FROM engrams WHERE tenant_id=? AND user_id=? AND created_at < ? AND importance < 1.5'
      : 'DELETE FROM engrams WHERE tenant_id=? AND user_id=?';
    const params = before ? [tenant_id, user_id, before] : [tenant_id, user_id];
    return { deleted: db.prepare(sql).run(...params).changes };
  }

  consolidate({ tenant_id, user_id }) {
    const rows = db.prepare('SELECT content FROM engrams WHERE tenant_id=? AND user_id=? ORDER BY updated_at DESC LIMIT 50').all(tenant_id, user_id);
    const summary = rows.map((r, i) => `${i + 1}. ${r.content}`).join('\n');
    db.prepare('INSERT INTO memory_summaries (id, tenant_id, user_id, summary, based_on_count, created_at) VALUES (?, ?, ?, ?, ?, ?)')
      .run(randomUUID(), tenant_id, user_id, summary, rows.length, now());
    return { summary, based_on: rows.length };
  }

  getSummaries({ tenant_id, user_id, limit = 5 }) {
    return db.prepare('SELECT * FROM memory_summaries WHERE tenant_id=? AND user_id=? ORDER BY created_at DESC LIMIT ?').all(tenant_id, user_id, limit);
  }
}

export default new EngramService();
```

**Step 4: Run test to verify pass**

Run: `node --test tests/engram.mjs`
Expected: PASS

**Step 5: Add to package.json**

```json
"test": "... tests/engram.mjs",
"checks": "... && node --check tests/engram.mjs"
```

**Step 6: Commit**

```bash
git add src/services/engramService.js tests/engram.mjs package.json
git commit -m "feat(engram): continuous memory with recall, consolidate and forget"
```

---

## Task 3: Hybrid GraphRAG + Engram Bridge

**Objective:** Conectar GraphRAG con memoria continua para respuestas híbridas.

**Files:**
- Create: `src/services/hybridRAGService.js`
- Test: `tests/hybrid-rag.mjs`

**Step 1: Write failing test**

```js
test('Hybrid RAG busca en KB y memoria', () => {
  engramService.remember({ tenant_id: 'hy1', user_id: 'u1', content: 'Mi impresora es HP LaserJet' });
  const r = hybridRAGService.search({ tenant_id: 'hy1', user_id: 'u1', query: 'impresora', topK: 5 });
  assert.ok(r.length > 0);
  assert.ok(r.some(x => x.source === 'memory'));
});
```

**Step 2: Run test to verify failure**
Expected: FAIL

**Step 3: Write implementation**

```js
import graphRAG from '../ml/graphRAG.js';
import engramService from './engramService.js';
import { findSimilarArticles } from '../ml/similaritySearch.js';

export function hybridSearch({ tenant_id, user_id, query, topK = 5 }) {
  const results = [];
  const kb = graphRAG.search({ tenant_id, query, limit: topK });
  for (const k of kb) results.push({ source: 'kb', id: k.article_id, score: k.score, matched: k.matched });
  const mem = engramService.recall({ tenant_id, user_id, query, topK });
  for (const m of mem) results.push({ source: 'memory', id: m.id, score: m.score, content: m.content, type: m.type });
  const vec = findSimilarArticles({ tenant_id, query, limit: topK });
  for (const v of vec) results.push({ source: 'vector', id: v.id, score: v.score, title: v.title });
  return results.sort((a, b) => b.score - a.score).slice(0, topK);
}
```

**Step 4: Commit**

```bash
git add src/services/hybridRAGService.js tests/hybrid-rag.mjs package.json
git commit -m "feat(rag): hybrid GraphRAG + engram + vector search"
```

---

## Task 4: Multimodal Router Enhancement

**Objective:** Extender el router AAAS para aceptar imágenes (base64) y futuro audio, con modelos multimodales.

**Files:**
- Modify: `src/services/aaasRouterService.js`
- Test: `tests/aaas-router.mjs`

**Step 1: Add multimodal payload normalization**

In `aaasRouterService.js`, add helper:

```js
function normalizeMessages(input) {
  if (typeof input === 'string') return [{ role: 'user', content: input }];
  if (Array.isArray(input)) return input;
  if (input.messages) return input.messages;
  return [{ role: 'user', content: input.prompt || JSON.stringify(input) }];
}

function attachMedia(messages, media = []) {
  if (!media || !media.length) return messages;
  const out = messages.slice();
  for (const m of media) {
    out.push({ role: 'user', content: [{ type: 'text', text: 'Analyze this media' }, { type: 'image_url', image_url: { url: m.data || m.url } }] });
  }
  return out;
}
```

**Step 2: Modify `generate` to accept `media`**

```js
async generate({ tenant_id, user_id, messages, prompt, media, strategy = 'balanced', ...opts }) {
  let msgs = normalizeMessages(messages || prompt);
  msgs = attachMedia(msgs, media);
  // rest of routing logic
}
```

**Step 3: Add test**

```js
test('router normaliza mensajes multimodales', () => {
  const r = aaasRouter.normalizeMessagesForTest ? aaasRouter.normalizeMessagesForTest({ prompt: 'hola' }) : [{ role: 'user', content: 'hola' }];
  assert.equal(r[0].role, 'user');
});
```

**Step 4: Commit**

```bash
git add src/services/aaasRouterService.js tests/aaas-router.mjs
git commit -m "feat(aaas): multimodal payload normalization for images"
```

---

## Task 5: Agent Swarm Robustness

**Objective:** Mejorar swarm con health check, reputación, claims y leader election simple.

**Files:**
- Modify: `src/services/swarmProtocolService.js`
- Modify: `src/services/db.js` (ya existe tabla de mensajes/claims)
- Test: `tests/swarm.mjs` (crear si no existe)

**Step 1: Add swarm methods**

```js
function electLeader(tenant_id, channel) {
  const nodes = db.prepare('SELECT from_agent, COUNT(*) as c FROM agent_messages WHERE tenant_id=? AND channel=? GROUP BY from_agent ORDER BY c DESC LIMIT 1').get(tenant_id, channel);
  return nodes ? nodes.from_agent : null;
}

function getReputation(tenant_id, agent_id) {
  const sent = db.prepare('SELECT COUNT(*) as c FROM agent_messages WHERE tenant_id=? AND from_agent=?').get(tenant_id, agent_id).c || 0;
  const claims = db.prepare('SELECT COUNT(*) as c FROM agent_claims WHERE tenant_id=? AND agent_id=? AND status="completed"').get(tenant_id, agent_id).c || 0;
  return { sent, claims, score: Math.min(1, (claims + 1) / (sent + 1)) };
}

export default { send, broadcast, channelMessages, electLeader, getReputation };
```

**Step 2: Commit**

```bash
git add src/services/swarmProtocolService.js tests/swarm.mjs
git commit -m "feat(swarm): leader election and reputation scoring"
```

---

## Task 6: Orchestrator Simplicio-Loop Integration

**Objective:** Que todos los agent runs del orquestador persistan en Engram y publiquen evidence gate.

**Files:**
- Modify: `src/services/orchestratorService.js`
- Test: `tests/orchestrator.mjs` (existing)

**Step 1: Inject engramService**

```js
import engramService from './engramService.js';
```

**Step 2: On `advance` to DONE or each gate, persist evidence**

```js
if (state === 'DONE' || state === 'VERIFY') {
  engramService.remember({ tenant_id, user_id, content: `Goal ${goal}: state ${state}. Evidence: ${JSON.stringify(evidence)}`, type: 'agent_run', importance: 2, summary: `Run ${id} reached ${state}` });
}
```

**Step 3: Commit**

```bash
git add src/services/orchestratorService.js
git commit -m "feat(orchestrator): persist agent runs into engram memory"
```

---

## Task 7: Computer Use Agent Integration

**Objective:** Exponer endpoint `/api/cua/agent-act` para que agentes ejecuten acciones de escritorio.

**Files:**
- Create: `src/services/cuaAgentService.js`
- Modify: `server.mjs`
- Test: `tests/cua-agent.mjs`

**Step 1: Service wrapper**

```js
import cuaService from './cuaService.js';

export async function agentAct({ tenant_id, agent_id, task, max_steps = 5 }) {
  const steps = [];
  for (let i = 0; i < max_steps; i++) {
    const capture = await cuaService.capture({ app: 'screen' });
    const decision = { action: 'done', reason: 'stub' }; // en v2.3.1 se conectará LLM
    steps.push({ step: i, capture_path: capture.path, decision });
    if (decision.action === 'done') break;
  }
  return { tenant_id, agent_id, task, steps };
}
```

**Step 2: Endpoint**

```js
if (pathname === '/api/cua/agent-act' && req.method === 'POST') {
  const result = await cuaAgentService.agentAct({ tenant_id: user.tenant_id, agent_id: user.id, task: body.task });
  return json(res, { success: true, result });
}
```

**Step 3: Commit**

```bash
git add src/services/cuaAgentService.js tests/cua-agent.mjs server.mjs
git commit -m "feat(cua): agent-act endpoint for desktop automation"
```

---

## Task 8: ML / Deep Learning Models para Soporte TI

**Objective:** Añadir modelos locales de clasificación y predicción de tickets.

**Files:**
- Create: `src/ml/ticketML.js`
- Test: `tests/ticket-ml.mjs`

**Step 1: Implementar simple MLP-like classifier con ml-matrix**

```js
import { Matrix } from 'ml-matrix';

const VOCAB = ['password','vpn','printer','email','slow','error','access','install','update','network'];

function featurize(text) {
  const t = String(text).toLowerCase();
  return VOCAB.map(v => t.includes(v) ? 1 : 0);
}

export function trainClassifier(samples) {
  const X = new Matrix(samples.map(s => featurize(s.text)));
  const y = Matrix.columnVector(samples.map(s => s.label));
  // stub: return weights vector for linear scoring
  const weights = VOCAB.map((v, i) => {
    const pos = samples.filter(s => s.label === 1 && featurize(s.text)[i]).length;
    return pos / (samples.length || 1);
  });
  return { weights, vocab: VOCAB };
}

export function predict(model, text) {
  const f = featurize(text);
  let score = 0;
  for (let i = 0; i < f.length; i++) score += f[i] * model.weights[i];
  const labels = ['password-reset','vpn','printer','other'];
  return { predicted: labels[Math.floor(score * labels.length) % labels.length], score };
}
```

**Step 2: Commit**

```bash
git add src/ml/ticketML.js tests/ticket-ml.mjs package.json
git commit -m "feat(ml): local ticket classifier with ml-matrix"
```

---

## Task 9: Enterprise UI — AaaS Operations Center

**Objective:** Unificar todo en una sola vista enterprise con tabs: Agents, Memory, RAG, Swarm, ML, CUA.

**Files:**
- Modify: `public/static/app.js`
- Modify: `public/index.html`

**Step 1: Register new renderers**

```js
  agents: () => renderAgentsHub,
  memory: () => renderMemoryHub,
  ml: () => renderMLHub,
  cua: () => renderCUA,
```

**Step 2: Add nav**

```html
<a onclick="show('agents')">🤖 Agents</a>
<a onclick="show('memory')">🧠 Memory</a>
<a onclick="show('ml')">🧮 ML</a>
```

**Step 3: Render functions**

```js
async function renderMemoryHub(el) {
  el.innerHTML = `
    <h2>🧠 Memory Hub</h2>
    <div class="card">
      <input id="mem-query" placeholder="Buscar memoria..." />
      <button onclick="searchMemory()">Buscar</button>
      <pre id="mem-results"></pre>
    </div>`;
}
async function searchMemory() {
  const q = document.getElementById('mem-query').value;
  const r = await api('GET', '/api/memory/engram?query=' + encodeURIComponent(q));
  document.getElementById('mem-results').textContent = JSON.stringify(r, null, 2);
}
```

**Step 4: Commit**

```bash
git add public/static/app.js public/index.html
git commit -m "feat(ui): enterprise agents, memory and ml hubs"
```

---

## Task 10: Endpoints REST Unificados

**Objective:** Exponer engram, hybrid RAG, swarm reputation, ML predict y CUA agent-act.

**Files:**
- Modify: `server.mjs`

**Step 1: Add routes**

```js
import engramService from './src/services/engramService.js';
import * as hybridRAG from './src/services/hybridRAGService.js';
import swarmProtocol from './src/services/swarmProtocolService.js';
import * as ticketML from './src/ml/ticketML.js';
import * as cuaAgentService from './src/services/cuaAgentService.js';

// Engram
if (pathname === '/api/memory/engram' && req.method === 'GET') {
  return json(res, { success: true, memories: engramService.recall({ tenant_id: user.tenant_id, user_id: user.id, query: url.searchParams.get('query') || '', topK: 10 }) });
}
if (pathname === '/api/memory/engram' && req.method === 'POST') {
  const id = engramService.remember({ tenant_id: user.tenant_id, user_id: user.id, ...body });
  return json(res, { success: true, id });
}

// Hybrid RAG
if (pathname === '/api/rag/hybrid' && req.method === 'GET') {
  return json(res, { success: true, results: hybridRAG.hybridSearch({ tenant_id: user.tenant_id, user_id: user.id, query: url.searchParams.get('query') || '' }) });
}

// Swarm reputation
if (pathname === '/api/swarm/leader' && req.method === 'GET') {
  return json(res, { success: true, leader: swarmProtocol.electLeader(user.tenant_id, url.searchParams.get('channel') || 'general') });
}
if (pathname === '/api/swarm/reputation/:agent_id' && req.method === 'GET') {
  const agent_id = pathname.split('/')[4];
  return json(res, { success: true, reputation: swarmProtocol.getReputation(user.tenant_id, agent_id) });
}

// ML
if (pathname === '/api/ml/predict' && req.method === 'POST') {
  const model = ticketML.trainClassifier(body.samples || []);
  return json(res, { success: true, prediction: ticketML.predict(model, body.text) });
}

// CUA agent act
if (pathname === '/api/cua/agent-act' && req.method === 'POST') {
  const result = await cuaAgentService.agentAct({ tenant_id: user.tenant_id, agent_id: user.id, task: body.task });
  return json(res, { success: true, result });
}
```

**Step 2: Commit**

```bash
git add server.mjs
git commit -m "feat(api): unified engram, hybrid rag, swarm, ml and cua endpoints"
```

---

## Task 11: Smoke Tests Extendidos

**Objective:** Añadir checks para todos los nuevos endpoints.

**Files:**
- Modify: `tests/smoke.mjs`

**Step 1: Add checks**

```js
add('POST /api/memory/engram', (await request('POST', '/api/memory/engram', { content: 'test memory', type: 'episodic' }, token)).status === 200);
add('GET /api/memory/engram', (await request('GET', '/api/memory/engram?query=test', null, token)).status === 200);
add('GET /api/rag/hybrid', (await request('GET', '/api/rag/hybrid?query=password', null, token)).status === 200);
add('POST /api/ml/predict', (await request('POST', '/api/ml/predict', { text: 'I forgot my password', samples: [{ text: 'password reset', label: 1 }] }, token)).status === 200);
add('GET /api/swarm/leader', (await request('GET', '/api/swarm/leader?channel=general', null, token)).status === 200);
```

**Step 2: Commit**

```bash
git add tests/smoke.mjs
git commit -m "test(smoke): engram, rag, ml and swarm checks"
```

---

## Task 12: Documentación Comercial y Packaging .exe

**Objective:** Crear README comercial, ADR y script de build de .exe.

**Files:**
- Modify: `README.md`
- Create: `docs/ADR-022-aaaas-unified.md`
- Modify: `package.json` scripts
- Create: `scripts/build-exe.mjs`

**Step 1: Add build-exe script**

```js
import { execSync } from 'child_process';
import { existsSync, mkdirSync } from 'fs';

if (!existsSync('dist')) mkdirSync('dist');
try {
  execSync('npx pkg . --out-path dist/ --targets node20-win-x64', { stdio: 'inherit' });
  console.log('✅ .exe generado en dist/azurdesk-ai.exe');
} catch (e) {
  console.log('⚠️ pkg no disponible; alternativa: npm install -g pkg');
  process.exit(1);
}
```

Add to `package.json`:

```json
"build:exe": "node scripts/build-exe.mjs"
```

**Step 2: ADR-022**

```markdown
# ADR-022: Agents-as-a-Service + SaaS Unificado

## Contexto
AzurDesk AI evoluciona de helpdesk a plataforma híbrida AaaS+SaaS.

## Decisiones
1. Engram/mem0-style memory en SQLite.
2. GraphRAG + vector + memory híbrido.
3. Router multimodal con media attachments.
4. Swarm con reputación y leader election.
5. ML local con ml-matrix.
6. Packaging .exe para venta comercial.

## Consecuencias
- Producto completamente autónomo y vendible.
- No depende de infraestructura externa para core.
```

**Step 3: README comercial update**

Highlight:
- Agents-as-a-Service + SaaS
- 80+ endpoints
- Multi-LLM, multimodal, Computer Use
- Engram memory, GraphRAG, ML
- One-click Windows .exe

**Step 4: Commit**

```bash
git add README.md docs/ADR-022-aaaas-unified.md scripts/build-exe.mjs package.json
git commit -m "docs(commercial): README, ADR-022 and .exe build script"
```

---

## Task 13: Version Bump y Evidence Gate Final

**Objective:** Subir a v2.3.0 y verificar todo.

**Files:**
- Modify: `package.json`
- Modify: `CHANGELOG.md`

**Step 1: Bump**

```json
"version": "2.3.0"
```

**Step 2: CHANGELOG**

```markdown
## [2.3.0] - 2026-06-27 — Agents-as-a-Service + SaaS Unified
- Engram continuous memory (mem0-style)
- Hybrid GraphRAG + vector + memory
- Multimodal AAAS router
- Agent swarm reputation + leader election
- Orchestrator persists runs to memory
- ML ticket classifier
- CUA agent-act endpoint
- Enterprise UI: Agents, Memory, ML hubs
- Commercial .exe build script
```

**Step 3: Evidence Gate**

Run:
```bash
cd /c/Users/Manu/azurdesk-ai
npm run build
```
Expected: 140+ tests, todos pass excepto skip de Obsidian.

Run server + smoke:
```bash
node server.mjs &
sleep 3
node tests/smoke.mjs
```
Expected: 80/80 checks pass.

Kill server:
```bash
tskill $(netstat -ano | grep LISTENING | grep 5200 | head -1 | awk '{print $NF}')
```

**Step 4: Commit**

```bash
git add package.json CHANGELOG.md
git commit -m "chore: v2.3.0 AaaS+SaaS unified release"
```

---

## Resumen

| Task | Archivos | Verificación |
|------|----------|--------------|
| 1 | `src/services/db.js` | syntax check |
| 2 | `src/services/engramService.js`, `tests/engram.mjs` | unit test |
| 3 | `src/services/hybridRAGService.js`, `tests/hybrid-rag.mjs` | unit test |
| 4 | `src/services/aaasRouterService.js`, `tests/aaas-router.mjs` | unit test |
| 5 | `src/services/swarmProtocolService.js`, `tests/swarm.mjs` | unit test |
| 6 | `src/services/orchestratorService.js` | unit test |
| 7 | `src/services/cuaAgentService.js`, `tests/cua-agent.mjs`, `server.mjs` | unit test |
| 8 | `src/ml/ticketML.js`, `tests/ticket-ml.mjs` | unit test |
| 9 | `public/static/app.js`, `public/index.html` | smoke + manual |
| 10 | `server.mjs` | smoke |
| 11 | `tests/smoke.mjs` | smoke |
| 12 | `README.md`, `docs/ADR-022-aaaas-unified.md`, `scripts/build-exe.mjs` | review |
| 13 | `package.json`, `CHANGELOG.md` | full build + smoke |

**Notas de riesgo:**
- `pkg` requiere instalación global o se puede usar `nexe` alternativo.
- Computer Use real depende de CUA driver funcionando en Windows.
- Multimodal real requiere modelos con visión en providers configurados.

**Entregable final:**
- Código funcional v2.3.0
- `.exe` generado en `dist/azurdesk-ai.exe`
- Documentación comercial lista
- Tests verificados
